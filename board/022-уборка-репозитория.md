# 022 · Уборка: `PROJECT_STATE-1.md`, мёртвые страницы, мёртвые ветки

**Статус:** новая
**Кому:** любой свободный чат
**Ветка:** `work/repo-cleanup`, от свежего `main`
**Раздел PROJECT_STATE:** **§170** — номер выдан оркестратором
**Заведено:** 2026-09-13

## Что сделать — только после проверки, не на глаз

1. **`PROJECT_STATE-1.md` (986 КБ).** Выяснить по `git log`, что это:
   старая копия, ветка истории, случайный дубль. Если его содержимое
   целиком есть в `PROJECT_STATE.md` — удалить и сказать, каким
   способом проверено (diff по разделам, не по размеру). Если там есть
   разделы, которых нет в основном, — **не удалять**, доложить.
2. **`HomeworksPage.tsx`** — не роутится (CLAUDE.md, «хвосты»); grep по
   импортам и тестам; мёртв — удалить вместе с тестом копии, если он
   есть (урок §149.1: тест мёртвого файла выглядит как покрытие).
3. **Ветки на GitHub, слитые в `main`** (`work/admin-structure`,
   `work/live-dashboard`, `work/live-cards`, `work/distribute-fix`,
   `work/plural-topics`, `work/clear-marks`, `work/tg-link-mobile`,
   `work/mobile-overflow`, `work/mobile-homework-submit`,
   `work/queue-topic-filter`, `work/catalog-mobile`, `work/topic-tasks`
   и старые: `speed-insights`, `video-stats`, `teacher-student-link`,
   `telegram-link-ux`, `student-course-counters`,
   `group-capacity-default`, `distribute-existing-group-only`,
   `review-solution-pane`) — **список** тех, у которых
   `git branch -r --merged main` подтверждает слияние. Удалять ветки
   будет владелец (пуш/удаление на GitHub — его), тебе — только список
   с подтверждением по каждой. Несданные (`ai-check-quality`,
   `study-plan`, `fab-overlap`, `attach-from-catalog`, `claude-md`,
   `presence-grace`, `demo-accounts-out`) в список не попадают.

## Приёмка

Отчёт с тремя списками и способом проверки каждого пункта.
`npx tsc -b > log 2>&1; echo $?` и тесты затронутых папок — зелёные.
