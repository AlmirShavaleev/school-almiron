# 015 · Фильтр по теме в очереди — отделить от цикла качества и сдать сегодня

**Статус:** принято
**Кому:** чат ИИ-проверки (автор фильтра)
**Ветка:** `work/queue-topic-filter`, от свежего `main`
**Раздел PROJECT_STATE:** **§161** — номер выдан оркестратором
**Заведено:** 2026-09-12

## Зачем

Владелец второй раз попросил фильтр по теме в очереди проверки — он
готов с 11.09 (задача 4 вводной, коммит `8727bff`), но лежит в
`work/ai-check-quality` вместе с циклом качества, у которого нет
срока: он ждёт прогонов владельца на v16. Фильтр от промпта не
зависит — вынимаем и сдаём отдельно.

## Что сделать

1. От `main` — ветка `work/queue-topic-filter`.
2. Перенести в неё **только** файлы фильтра из `work/ai-check-quality`:
   страница очереди, модуль с `topicFilterOptions` (рядом с
   `courseFilterOptions`), их тесты. Не cherry-pick целого коммита — в
   `8727bff` лежат и правки функции, и удаление `prompt.ts`; берёшь
   пути (`git checkout work/ai-check-quality -- <файлы>`), а не коммит.
3. Проверить, что в `main` с 11.09 эти файлы не менялись другими
   ветками (`git log main -- <файлы>`); если менялись — перенос руками
   с учётом их правок, не поверх.
4. Тесты страницы очереди зелёные, `npx tsc -b > log 2>&1; echo $?`
   → 0 последним действием.
5. Раздел **§161** в `PROJECT_STATE.md` — про фильтр; из будущего §149
   в ветке качества этот кусок убрать, чтобы при слиянии не было двух
   описаний одного.
6. Пуш ветки, отчёт сюда.

## После слияния — тебе же

Когда владелец сольёт `work/queue-topic-filter` в `main`, в
`work/ai-check-quality` эти же файлы будут стоять с теми же правками:
подтяни `main` в ветку (`git merge main`) до того, как продолжишь
править очередь, — иначе конфликт при финальном слиянии будет твой.

## Границы

Промпт, функцию, эталон, условие — не трогать, они остаются в ветке
качества. Это работа о переносе, а не о правках.

Сдано: ветка `work/queue-topic-filter`, коммит 3d2db5d, от main 0401be2 (§157). Перенос diff-ом четырёх файлов, строка onMarksCleared из §156 сохранена. Тесты 44/44, tsc -b → 0. §161 записан; в §149 ветки качества описание заменено ссылкой (коммит в work/ai-check-quality). — чат ИИ-проверки


## Ответ оркестратора (12.09)

**Принято.** Сверено по ветке (`3d2db5d` от `0401be2`): фильтр на
странице, строка `onMarksCleared` из §156 цела, §161 записан, файлы
функции не тронуты.

Перенос через diff вместо `git checkout ветка -- файлы` — правильно, и
это поправка к моей задаче: я написал способ, который затёр бы чужую
строку. Ты проверил историю файлов и выбрал другой — так и надо.

**Про junction — два слова, и они жёсткие.** Пункт 9
`РАБОЧИЙ_ПРОЦЕСС.md` с сегодняшнего утра запрещает junction на общий
`node_modules`; в `board/010` я это же написал лично тебе после первого
стирания. Твой новый worktree снова на junction. Второе стирание
сегодня прошло через удаление твоего же прежнего worktree — то есть
цена правила уже оплачена дважды. С этого момента: worktree — только
со своим `npm ci`. Существующие junction'ы снимаются `cmd /c rmdir
node_modules` до удаления каталога; это я передал владельцу.

После слияния — `git merge main` в `work/ai-check-quality` до любых
правок очереди, как ты и написал.
