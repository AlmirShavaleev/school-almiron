# 018 · CLAUDE.md: заменить на выверенную версию

**Статус:** новая
**Кому:** любой свободный чат
**Ветка:** `work/claude-md`, от свежего `main`
**Раздел PROJECT_STATE:** **§166** — номер выдан оркестратором
**Заведено:** 2026-09-12

## Зачем

`CLAUDE.md` читает каждый чат на старте. Аудит оркестратора 12.09 нашёл
в нём строки, которые **вредят**, а не помогают:

| Было | Почему неверно |
|---|---|
| «Тесты: `npx tsc --noEmit`, …» | в этом репозитории `--noEmit` не проверяет ничего; правильная команда `npx tsc -b` — это записано во всех вводных, но файл, который читают первым, говорит обратное |
| «Разделы §21–§33 — последняя сессия» | последний раздел — §165 |
| «схему проверять на локальном Postgres 16» | локального Postgres у чатов нет, пробы прав идут на проде через MCP |
| `providers.ts` читает `GEMINI_API_KEY` | `providers.ts` удалён (§149.1) |
| «Гонка в `course_join_accept`: `groups.course_id` без уникального ограничения» | закрыто §61/§64, индекс есть |
| нет ни слова про `ОРКЕСТРАЦИЯ.md`, `РАБОЧИЙ_ПРОЦЕСС.md`, доску, харнесс, запрет демо-учёток, общие определения | всё это появилось после последней правки файла |

## Что сделать

1. Взять `CLAUDE_НОВЫЙ.md` из корня (лежит на диске, в git не входит) и
   положить его содержимое в `CLAUDE.md`.
2. Прочитать диффом и **проверить каждое утверждение новой версии по
   коду и по проду**, а не на слово оркестратору: имена функций, номера
   разделов, пути файлов. Найдёшь неточность — исправь и назови в
   отчёте.
3. `CLAUDE_НОВЫЙ.md` после этого удалить с диска.
4. Раздел §166 — коротко: что убрано, что добавлено, почему.

## Границы

Только `CLAUDE.md`. Ничего в коде.

`npx tsc -b > log 2>&1; echo $?` последним (файл на типы не влияет,
правило одно для всех). Пуш ветки, отчёт сюда.
