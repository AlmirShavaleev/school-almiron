import { useSchoolPresence } from '@/hooks/useSchoolPresence'

/**
 * Отметка присутствия. Ничего не рисует.
 *
 * Монтируется ОДИН раз на всё защищённое поддерево (`AppRoutes`) — отмечаться
 * обязаны все вошедшие, а не только тот, кто открыл панель админа: список
 * читает админ, но если ученик не отметится, показывать будет нечего.
 *
 * В отметку идут только `profile_id` (из `auth.uid()`, аргументом не
 * задаётся), роль (из профиля, а не от клиента) и время. Ни адреса страницы,
 * ни идентификатора темы — «на каком экране находится ребёнок» владелец
 * запретил показывать, и этого нет в данных.
 */
export function SchoolPresencePublisher() {
  useSchoolPresence()
  return null
}
