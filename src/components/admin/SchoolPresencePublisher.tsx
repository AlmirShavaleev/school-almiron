import { useSchoolPresence } from '@/hooks/useSchoolPresence'

/**
 * Публикация присутствия. Ничего не рисует.
 *
 * Монтируется ОДИН раз на всё защищённое поддерево — присутствие обязаны
 * отправлять все вошедшие, а не только тот, кто открыл панель админа: без
 * ученического `track()` «кто сейчас онлайн» показал бы одних админов.
 *
 * В канал уходят только `profileId` и роль. Ни имени, ни адреса страницы, ни
 * идентификатора темы — «на каком экране находится ребёнок» владелец запретил
 * показывать, и здесь это не «не показываем», а «не отправляем вовсе».
 *
 * Второе монтирование безопасно: канал держится на счётчике ссылок
 * (`acquirePresence`), поэтому два публикатора дадут один сокет и одну запись
 * присутствия, а не двойника.
 */
export function SchoolPresencePublisher() {
  useSchoolPresence()
  return null
}
