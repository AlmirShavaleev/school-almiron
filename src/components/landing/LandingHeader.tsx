import {useEffect,useState} from 'react';import {AnimatePresence,motion} from 'framer-motion';import {Link} from 'react-router-dom';import {Menu,X} from 'lucide-react'
export function Brand(){return <a className="al-brand" href="#top"><b>A</b><span><strong>School Almiron</strong><small>Физика · Математика · ОГЭ · ЕГЭ</small></span></a>}
export function LandingHeader(){const[open,setOpen]=useState(false);const[compact,setCompact]=useState(false);useEffect(()=>{const fn=()=>setCompact(scrollY>28);fn();addEventListener('scroll',fn,{passive:true});return()=>removeEventListener('scroll',fn)},[]);const links=[['#story','Как работает'],['#platform','Платформа'],['#formats','Форматы'],['#results','Результаты'],['#teacher','Преподаватель'],['#faq','FAQ']];return <header className={`al-header ${compact?'is-compact':''}`}><div className="al-wrap al-nav"><Brand/><nav>{links.map(([h,l])=><a key={h} href={h}>{l}</a>)}<Link to="/login">Войти</Link><a className="al-btn al-btn--small" href="#diagnostic">Диагностика</a></nav><button className="al-menu-button" onClick={()=>setOpen(v=>!v)} aria-expanded={open} aria-controls="mobile-nav" aria-label={open?'Закрыть меню':'Открыть меню'}>{open?<X/>:<Menu/>}</button></div><AnimatePresence>{open&&<motion.nav id="mobile-nav" className="al-mobile-nav" initial={{opacity:0,y:-12}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-12}}>{links.map(([h,l])=><a key={h} href={h} onClick={()=>setOpen(false)}>{l}</a>)}<Link to="/login">Войти</Link><a className="al-btn" href="#diagnostic">Записаться на диагностику</a></motion.nav>}</AnimatePresence></header>}
