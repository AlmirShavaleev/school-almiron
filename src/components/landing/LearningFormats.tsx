import {useState} from 'react';import {AnimatePresence,motion} from 'framer-motion';import {Check,Users,UserRound} from 'lucide-react';import {MotionReveal} from './MotionReveal'
const formats=[{name:'Индивидуально',icon:UserRound,title:'Маршрут строится вокруг одного ученика',items:['Персональный темп','Индивидуальная траектория','Глубокая работа с пробелами','Постоянная обратная связь']},{name:'Мини-группа',icon:Users,title:'До 5 учеников — прогресс каждого под контролем',items:['Совместный разбор','Соревновательный элемент','Живая динамика группы','Персональный контроль']}]
export function LearningFormats(){const[active,setActive]=useState(0);const f=formats[active];const Icon=f.icon;return <section className="al-formats" id="formats"><div className="al-wrap"><MotionReveal className="format-heading"><span className="al-label dark">Форматы обучения</span><h2>Один подход.<br/>Разный ритм.</h2></MotionReveal><div className="format-switch" role="tablist">{formats.map((x,i)=><button role="tab" aria-selected={active===i} key={x.name} onClick={()=>setActive(i)}>{x.name}<span>0{i+1}</span></button>)}</div><AnimatePresence mode="wait"><motion.div className="format-stage" key={active} initial={{opacity:0,x:25}} animate={{opacity:1,x:0}} exit={{opacity:0,x:-25}}><div><Icon/><h3>{f.title}</h3><ul>{f.items.map(x=><li key={x}><Check/>{x}</li>)}</ul><a href="#diagnostic">Узнать условия</a></div><div className="format-orbit"><span className="center-node">{active===0?'Ученик':'Группа'}</span>{f.items.map((x,i)=><span className={`node n${i+1}`} key={x}>{x}</span>)}<svg viewBox="0 0 500 340"><ellipse cx="250" cy="170" rx="190" ry="115"/></svg></div></motion.div></AnimatePresence></div></section>}
