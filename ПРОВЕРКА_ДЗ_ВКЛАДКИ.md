# Задание: страница «Проверка ДЗ» — показать все состояния работ

Вводная для нового чата, от владельца через оркестратора, 2026-08-04.
Читать ПЕРВЫМИ: `CLAUDE.md` (общие правила проекта) и `ОРКЕСТРАЦИЯ.md`
(зоны параллельных чатов, реестр разделов PROJECT_STATE). Ты принимаешь
зону контура проверки ДЗ — прежний чат этой зоны потерян, его работа
описана в PROJECT_STATE (§43, §48, §54 и рядом).

## Повод (скриншот владельца)

Страница «Проверка домашних заданий» (режим учителя) показывает ТОЛЬКО
работы «Ожидает проверки». Принятые и отправленные на доработку с неё
невидимы — а их большинство. По проду на 04.08 в
`topic_homework_attempts`: `submitted` 3, `returned_for_revision` 6,
`accepted` 9. Владелец: «не видно, какие на доработке, какие приняты.
Нужны все типы».

## Что сделать

1. Переключатель состояний на странице: **«Ждут проверки / На доработке /
   Принятые»** (вкладки или фильтр — реши по месту), у каждой видно
   число работ.
2. Строки не-в-ожидании — тот же формат карточки; бейдж состояния уже
   существует; вместо кнопки «Проверить» — «Открыть» (переоткрыть
   проверку/посмотреть вердикт).
3. Существующие фильтры (курс, порядок, «только просроченные») работают
   во всех вкладках.
4. По умолчанию открыта «Ждут проверки» — текущее поведение не ломать.
5. Счётчик в шапке («Ждут проверки: 3») оставить про ожидающих.

## Осторожно — грабли этой зоны (уже стреляли)

- В режиме учителя владелец — admin с строкой teachers; выборки должны
  сужаться по «роли представления» `useEffectiveRole()`, НЕ по
  `profile.role` (§78/§80). Если страница грузит работы без фильтра
  «только мои курсы» в расчёте на RLS — админу покажется вся школа.
  Проверь и почини заодно, если так.
- Значки ИИ-проверки на строках: карта задач в `useQueueAiJobs.ts`
  сливается merge'ем, не replace (иначе значок остаётся только у
  последней строки — уже чинили). Не сломай при перестройке списка.
- `notification_queue` и всё телеграмное — чужая зона (уведомления).
  Вкладки не должны рождать событий.

## Правила (полные в CLAUDE.md и карте)

- Источник правды — диск `D:\Школа Almiron`, git pull перед началом.
- Миграции (если вдруг понадобятся — тут вряд ли): только MCP
  `apply_migration` + файл в `supabase/migrations/` с именем, точно
  равным версии в `schema_migrations`. Никогда `supabase db push`.
- Номер раздела PROJECT_STATE: последний реальный `## §N` в файле + 1 в
  момент записи (`grep '^## §' PROJECT_STATE.md | tail -3`). Сейчас
  хвост в районе §81 — проверь сам.
- Полный `npx tsc --noEmit > log 2>&1; echo $?` перед отчётом — без
  конвейеров с head. Плюс `npx vite build` и точечные vitest по
  затронутым файлам.
- Пушит владелец. Отчёт — оркестратору: что нашёл, что изменил, какие
  файлы, номер раздела.
