# Гигиена до сентября — вводная

Админ-чату, от владельца через оркестратора, 2026-08-08. Читать после
`CLAUDE.md` и `ОРКЕСТРАЦИЯ.md`. Работа опасная: тут удаляются данные в
проде. Дисциплина строже обычного — см. «Порядок».

## Состояние (сверено оркестратором по проду 08.08)

Курсы сейчас:

| Курс | Тем | Учеников |
|---|---|---|
| Физика ЕГЭ Шаблон | 169 | 0 |
| Математика ЕГЭ. 1 часть + джентльменский набор — Шаблон | 83 | 0 |
| Математика ЕГЭ. Вторая часть — Шаблон | 130 | 0 |
| Физика ЕГЭ 11А класс | 169 | 0 |
| Тестовый курс | 5 | 1 |
| 10 | 2 | 0 |
| егэ | 0 | 0 |

Первые четыре — боевые, не трогать. Кандидаты на снос: **«Тестовый
курс», «10», «егэ»**. Владелец часть мусора уже убрал сам.

Ещё: **24 записи в `auth.users` без профиля** (демо и e2e), и шесть
таблиц-снимков отката: `groups_cleanup_backup_20260803`,
`group_students_cleanup_backup_20260803`,
`enrollment_invites_cleanup_backup_20260803`,
`catalog_topics_title_backup_20260803`,
`catalog_task_topics_rehang_backup_20260803`,
`catalog_tasks_multichoice_backup_20260805`.

## Что сделать

1. **Список на подтверждение.** Сначала собери и покажи оркестратору
   полный список к удалению: курсы с их модулями/темами/материалами и
   объёмом файлов, аккаунты (кто именно, по email), таблицы-снимки.
   Ничего не удалять до «да».
2. **Курсы** — сносить штатным путём удаления курса, не руками по
   таблицам: там каскады, объекты Storage и (после §102) счётчик ссылок.
3. **Аккаунты без профиля** — разобраться, почему они возникли (регистрация,
   не создавшая профиль? удалённые профили?), и снести. Если найдётся
   живой сценарий, который их плодит, — это находка, доложи.
4. **Снимки отката:** три «групповых» (03.08) снести — закон 1:1 живёт
   пятый день, откатывать некуда и незачем. Два каталожных (переименование
   тем и перевес) — **только после того, как владелец подтвердит, что
   списки тем в каталоге выглядят правильно**; он этого ещё не говорил,
   спроси через оркестратора. `catalog_tasks_multichoice_backup_20260805`
   оставить до сентября — правка свежая.

## Жёсткое условие по срокам

**Удаление курсов делать ТОЛЬКО после того, как §102 (общие файлы и
счётчик ссылок) окажется в проде.** Иначе удаление копии может выбить
объекты, на которые ссылается шаблон, — а «Физика ЕГЭ 11А класс» это
именно копия. Проверь по коду на диске и у оркестратора, доехал ли §102;
если нет — начни с аккаунтов и снимков, курсы отложи.

## Порядок

1. Всё удаление — с предварительным снимком затронутых строк
   (`*_backup_<дата>`, revoke у ролей), как делалось в §50 и §61.
2. Никаких «удалю и посмотрю»: сначала запрос-подсчёт, потом показ
   оркестратору, потом удаление, потом контрольный подсчёт.
3. Миграции — MCP `apply_migration` + файл с точным именем; данные
   можно и `execute_sql`, но снимок обязателен.
4. Номер раздела — хвост+1 в момент записи. Полный
   `npx tsc --noEmit > log 2>&1; echo $?` последним действием, если
   трогал код (скорее всего не понадобится).
5. В отчёте — цифрами: сколько строк, объектов и мегабайт освобождено,
   что осталось намеренно.
