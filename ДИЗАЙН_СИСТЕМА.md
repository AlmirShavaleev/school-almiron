# Дизайн-система «Школы Almiron» — вводная для дизайн-чата

От владельца через оркестратора, 2026-08-04. Это задание БЕЗ КОДА:
результат — документ (аудит + дизайн-система), ни одного файла в
`src/` не менять. Применение по коду будет отдельным заходом позже,
когда закончатся функциональные работы других чатов.

## Продукт

Онлайн-школа подготовки к ЕГЭ/ОГЭ по физике и математике (прод
alminion.ru). Владелец Альмир — админ и преподаватель в одном лице.
Аудитория: школьники 8–11 классов (основные пользователи), преподаватели
и кураторы (проверка работ), админ. Школьники сидят и с телефонов —
мобильная вёрстка важна.

Технически: React + Tailwind. Итоговые токены системы должны ложиться
на Tailwind (палитра, шкала отступов, радиусы, тени, типографика) —
чтобы применение было механическим.

## Что есть сейчас (главные экраны)

Владелец пришлёт скриншоты по запросу — проси конкретные экраны.

- Вход → экран выбора режима (админ/учитель) → кабинеты.
- Сайдбар-навигация: Центр управления, Учебный процесс (Программа
  курса, Ученики), Задания (Каталог, Проверка ДЗ, Тесты, Банк тестов).
- Программа курса: темы с тумблером «Открыта/Закрыта», модалка ДЗ
  (загрузка PDF/картинок, дедлайн, баллы, оповещение в Telegram).
- Проверка ДЗ: вкладки «Ждут/На доработке/Приняты», просмотрщик работ
  с ИИ-разметкой (рамки-комментарии поверх страниц).
- Каталог задач: дерево тем, тысячи задач, формирование PDF-подборок.
- Тесты: вход четырьмя картами экзаменов, автосборка вариантов,
  прохождение учеником.
- Ученики: таблица (ФИО·класс·группа·последняя сдача·TG·добавлен).
- Панель админа: девять плиток статистики школы.
- Кабинет ученика: темы курса, сдача ДЗ, тесты, результаты.
- Виджет «Сообщить о проблеме» (скриншоты, Ctrl+V).

## Известные боли владельца (из рабочей переписки, цитаты по смыслу)

- «Модалка назначения тестирования перегружена и неудобная».
- Карточки-простыни: список учеников был сеткой карточек с пустыми
  полями («Группы: —») — уже переделан в таблицу, направление верное.
- «Тяжеловато будет подобрать задачи» — формы с большим числом полей.
- Плитки/бейджи местами разнобойные: разные accept-подписи, разные
  стили кнопок в соседних местах.
- Обилие тестовых данных в интерфейсах — на дизайн не смотреть, будет
  зачистка.

## Что нужно на выходе (один документ, можно HTML)

1. **Аудит**: 10–15 главных проблем текущего UI по скриншотам, с
   приоритетами. Не «всё переделать», а конкретно: где теряется
   иерархия, где перегруз, где непоследовательность.
2. **Дизайн-система**:
   - палитра (светлая тема обязательно; тёмная — предложи, владелец
     решит), семантические цвета состояний (сдано/проверено/на
     доработке/просрочено — они в продукте повсюду);
   - типографика: шкала размеров, начертания, межстрочные;
   - отступы, радиусы, тени — шкалой;
   - компоненты: кнопка (первичная/вторичная/опасная), таблица, бейдж
     статуса, вкладки, модалка, карточка, поле формы, тумблер, пустое
     состояние, тост;
   - мобильное поведение ключевых экранов (таблицы → как сворачиваются).
3. **Токены под Tailwind** — таблицей «токен → значение», чтобы
   применение было правкой конфига, а не переизобретением.
4. Два-три эскиза «до/после» самых больных экранов (модалка
   тестирования, проверка ДЗ) — чтобы владелец увидел систему в деле.

## Правила

- Кода в репозитории НЕ трогать. Никаких правок `src/`, никаких пушей.
  Результат — файл(ы) документа в корень проекта (`ДИЗАЙН_АУДИТ.md` /
  `ДИЗАЙН_СИСТЕМА_ГАЙД.html`).
- В PROJECT_STATE.md НЕ писать — это не изменение продукта. Учёт ведёт
  оркестратор в своей карте.
- Вопросы владельцу — списком и порциями, не по одному.
- Русский язык интерфейса; тексты кнопок и подписей — короткие, без
  канцелярита.
