# Дизайн-система «Школы Almiron» — версия 2 «градиенты»

Действует с §225 (26.09.2026). Владелец выбрал в Claude Design версию 2
«градиенты» со шрифтом Manrope. Исходник макета — `ДИЗАЙН-V2/design-v2.html`
(в git не входит; значения брать из его inline-стилей, не на глаз с картинки),
принципы — `ДИЗАЙН-V2/ДИЗАЙН_БРИФ.md`.

Прежний текст этого файла (вводная для дизайн-чата от 04.08.2026) — в истории
git до §225.

## Принципы (из брифа — короче не сказать)

1. **Сначала вывод, потом подробности.** Экран отвечает «что происходит и что
   делать», таблицы — ниже.
2. **Одно главное действие на экран** — синяя кнопка. Остальные тише.
3. **Состояние видно формой**, а не только цветом и цифрой.
4. **Честные цифры**: среднее всегда с подписью «по N работам».
5. **Карточка — только для отдельного объекта.** Рамки вокруг блоков не рисуем.
6. **Цвет несёт смысл** (верно / неверно / частично / не сверено / не решено).
7. **Печать чёрно-белая**: ничего не различается только цветом.
8. Без плиток-KPI сверху страницы («Всего пробников: 0»).

## Токены (`tailwind.config.js`)

Старые имена палитр сохранены, значения новые — поэтому весь сайт подтянулся без
правки экранов. `gray` и `slate` Tailwind перенастроены на ту же шкалу, что
`graphite`.

| Токен | Значение | Где |
|---|---|---|
| фон | градиент `#e6efff → #fdf6e6 → #eaf2ff` (`canvas.*`) | `body::before`, неподвижный слой |
| поверхность | `#ffffff` | карточки, панели |
| текст | `graphite-900` `#12234a` | основной |
| текст вторичный | `graphite-500` `#55607a` | подписи, даты |
| подсказка/значок | `graphite-400` `#7c87a0` | ≥ 3:1, не для текста-смысла |
| контур поля | `graphite-300` `#c9d6ef` | поля, вторичная кнопка |
| линия | `graphite-200` `#e1e9f7` | разделители строк |
| действие | `primary-600` `#1f55e0`; градиент кнопки `action.from #2d67f5 → action.to #1f55e0` | главная кнопка, ссылки |
| меню | градиент `menu.top #1a3a80 → menu.bottom #0e1f47`, текст `menu.text #dfe7fb`, `menu.muted #b9c6e6` | боковое меню |
| акцент | `gold-300` `#ffc933` | активный пункт меню, выделение; текст на нём только тёмный |
| тень карточки | `shadow-card` `0 10px 30px rgba(31,85,224,.08)` | |
| тень действия | `shadow-action` `0 6px 16px rgba(31,85,224,.28)` | главная кнопка |
| радиусы | 4 ячейка · 8 поле · 999 кнопка · `rounded-card` 20 карточка | |
| отступы | 4 · 8 · 12 · 16 · 24 · 32 · 48 | шкала Tailwind 1/2/3/4/6/8/12 |

Шкалы целиком:

- `primary`: 50 `#eef3ff` · 100 `#dde8ff` · 200 `#c2d5ff` · 300 `#93b4ff` · 400 `#5b93ff` · 500 `#2d67f5` · 600 `#1f55e0` · 700 `#1a46bd` · 800 `#1a3a80` · 900 `#142d66` · 950 `#0e1f47`
- `gold`: 50 `#fff8e1` · 100 `#ffefbf` · 200 `#ffe08a` · 300 `#ffc933` · 400 `#ffab00` · 500 `#dc8f00` · 600 `#b06f00` · 700 `#8a5500` · 800 `#6b4200` · 900 `#553400`
- `graphite` = `gray` = `slate`: 50 `#f5f8fe` · 100 `#edf1f9` · 200 `#e1e9f7` · 300 `#c9d6ef` · 400 `#7c87a0` · 500 `#55607a` · 600 `#465169` · 700 `#364160` · 800 `#23305a` · 900 `#12234a` · 950 `#0b1733`

**Контраст** держит тест `src/__tests__/designTokens.test.ts`: ступени 500 и
темнее ≥ 4.5:1 на белом и на самом светлом месте фона; белый на кнопке действия
по всей длине градиента; тёмный на жёлтом; текст меню на обоих концах; текст
меток состояния на своей заливке. Меняете палитру — тест скажет, что сломалось.

Отступление от макета одно: градиент главной кнопки в макете начинается с
`#3b7bff`, а белый текст на нём — 3.8:1. Начало взято `#2d67f5` (4.8:1), на глаз
разницы нет.

## Шрифт

**Manrope 400–800**, файлы в репозитории (`src/assets/fonts/manrope/`, латиница,
кириллица, греческий — для π, α, Δ), подключение — `src/styles/manrope.css`.
Не Google Fonts: ученики в России, внешний CDN шрифтов там ненадёжен.
Caveat из макета («школа», «сейчас главное») не подключён — рукописный акцент
понадобится, когда его возьмёт какой-нибудь экран.

Цифры — `tabular-nums` по всему кабинету (стоит на `body`). Шкала из макета:
28/600 заголовок экрана · 20/600 раздел · 17/400 вывод · 15/400 текст ·
13/400 подпись · 12/500 метка (прописные, разрядка .03em). **На телефоне текст
не мельче 13.**

Лендинг живёт в своём стиле (`ЛЕНДИНГ_СТИЛЬ.md`) и остаётся на Inter —
`src/pages/LandingFonts.css` грузится только с ним. Печатные листы остаются на
Times New Roman (`@media print` в `index.css`).

## Компоненты (`src/components/ui/`)

- **Button** — «таблетка». `primary` — синяя с градиентом и тенью, одна на
  экран; недоступная — ровная светло-серая. `secondary` — белая в контуре
  `#c9d6ef`, при наведении контур синий. `ghost` — тихая, текстом. `danger`,
  `success` — прежние смыслы. Высота 48 на телефоне, 40 с `sm` (`size="sm"` —
  44/32). На синем фоне главная кнопка — жёлтая (`bg-gold-300
  text-graphite-900`), компонента под это пока нет.
- **Input / Select** — контур 1.5px `#c9d6ef`, радиус 8, высота 48/40, текст
  16 на телефоне (iOS не приближает страницу) и 15 на ноутбуке; фокус — синий
  контур; ошибка — красный контур и строка `✕ …` под полем.
- **Card** — белая, радиус 20, `shadow-card`, без рамки
  (`.platform-surface`). Кликабельная приподнимается.
- **Badge** — заливка без обводки, текст — тёмный оттенок той же краски.
  `success`/`warning`/`error` = краски «верно»/«частично»/«неверно».
- **StatCard** — плитка-KPI. В шаге 1 только перекрашена; убирать плитки —
  задача шагов экранов.
- **VerdictMark** — метка состояния проверки (ниже).

### Состояния проверки — `VerdictMark`

| Состояние | Форма | Цвет (`verdict.*`) | Заливка ячейки |
|---|---|---|---|
| верно (`ok`) | закрашенный круг с галкой | `#33854a` | `#e4f8e7` |
| неверно (`bad`) | закрашенный круг с крестом | `#b54b43` | `#ffe6e2` |
| частично (`part`) | круг, закрашенный наполовину | `#a97416` | `#fdecd1` |
| не сверено (`unk`) | пунктирный круг с «?» | `#4075aa` | `#e8f3ff` |
| не решено (`none`) | пустой круг (на экране с тире) | `#8a857c` | `#f1efea` |

Текст на заливке — `verdict.<имя>.ink` (≥ 4.5:1). Значок рисуется SVG, а не
фоном: фоны браузер по умолчанию не печатает, и «верно» на бумаге стало бы
пустым кругом. На печати метка чёрная, тире «не решено» убирается.
Вердикты таблицы проверки ДЗ переводятся в состояния через
`MARK_OF_REVIEW_VERDICT` (`correct→ok`, `wrong→bad`, `partial→part`,
`unchecked→unk`, `unsolved→none`). Уже стоит в таблице проверки
(`ReviewTaskTable`: кружок вердикта, список выбора, строка слепка ИИ) и в
разборе у ученика (`ReviewTaskList`).

## Раскладка

- **Фон** — градиент неподвижным слоем (`body::before`), на печати его нет.
- **Меню** — тёмно-синий градиент; пункт — «таблетка», активный жёлтый с
  тёмным жирным текстом; счётчик-бейдж: на тёмном — `white/16`, на жёлтом —
  `graphite-900/12`. Текущий пункт отмечен `data-active="true"` — по нему, а не
  по цвету, его ищут тесты.
- **Строка списка** — значок состояния слева, действие справа, тихое;
  разделитель `#e1e9f7`.
- **Таблица** — заголовок столбца 12/600 прописными, строки через линию
  `#e1e9f7`, цифры моноширинные.
- **Клетка таблицы с баллом** (§227, таблица пробника) — те же состояния, что у
  `VerdictMark`, но число в клетке, и у каждого своя форма, не только заливка:
  полный балл — без фона; частично — `part.tint` и черта снизу `part`; ноль —
  `bad.tint` и рамка `bad`; не сверено — пунктир `unk` и «?»; не решено — «—»
  серым. Слабый номер в итоговой строке — как ноль (заливка, рамка, жирный).
  Вид клетки — `cellLook` в `src/lib/mockExamGridView.ts`.

## Чего нет и что дальше

Шаг 1 — только основа. Экраны (раскладка, тексты, плитки-KPI, фиолетовые
акценты таблицы проверки, цветные рамки блоков, фиолетовый баннер курса) — по
одному в следующих шагах, по макетам `ДИЗАЙН-V2/v2-01…05.png`. Тёмная тема
из макета не включена.
