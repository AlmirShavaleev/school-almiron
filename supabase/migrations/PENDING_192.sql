-- §192 (board/045). Номер задания у находки ИИ-проверки. НЕ ПРИМЕНЕНО: файл
-- кладёт агент, применяет оркестратор через MCP и переименовывает по
-- фактической версии из supabase_migrations.schema_migrations.
-- Только добавляющее: старый код (в том числе задеплоенная сейчас функция)
-- столбца не знает и продолжает работать — он остаётся NULL.
-- ПОРЯДОК ВЫКАТА ЖЁСТКИЙ: сначала эта миграция, потом деплой функции v18+.
-- Обратный порядок роняет всю проверку: вставка находок идёт одним insert,
-- неизвестный столбец валит его целиком, а с §180 ошибка записи честно
-- переводит задачу в failed.
alter table public.topic_homework_ai_findings
  add column if not exists task text;

comment on column public.topic_homework_ai_findings.task is
  'Номер задания, к которому относится находка (v18+, §192): тот самый `task`, по которому функция держит лимит «одна находка на задание» (findings.ts, §180). Панель проверки связывает строку таблицы topic_homework_ai_jobs.tasks с находкой по этому столбцу; NULL — проверка старее §192, там связь восстанавливается из текста находки («В задаче 4…»).';
