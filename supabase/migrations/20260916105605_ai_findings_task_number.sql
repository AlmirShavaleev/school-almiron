-- §192 (board/045). Номер задания у находки ИИ-проверки. Применено
-- оркестратором через MCP 16.09.2026 (версия 20260916105605) ДО деплоя
-- функции с §192. Только добавляющее: функция без §192 столбца не знает и
-- оставляет его NULL. Обратный порядок уронил бы вставку находок целиком
-- (§180: ошибка записи переводит задачу в failed).
alter table public.topic_homework_ai_findings
  add column if not exists task text;

comment on column public.topic_homework_ai_findings.task is
  'Номер задания, к которому относится находка (v18+, §192): тот самый `task`, по которому функция держит лимит «одна находка на задание» (findings.ts, §180). Панель проверки связывает строку таблицы topic_homework_ai_jobs.tasks с находкой по этому столбцу; NULL — проверка старее §192, там связь восстанавливается из текста находки («В задаче 4…»).';
