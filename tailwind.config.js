/**
 * §225. Токены нового дизайна (версия 2 «градиенты», Manrope) — шаг 1, основа.
 *
 * Значения сняты с макета `ДИЗАЙН-V2/design-v2.html` (inline-стили), а не на
 * глаз с картинки. Имена палитр — прежние (`primary`, `gold`, `graphite`), и
 * вдобавок перенастроены стандартные `gray` и `slate`: по сайту это ~4000
 * классов, и перекраска через конфиг подтягивает все экраны сразу, без правки
 * сотен файлов. Экраны перестраиваются отдельными шагами.
 *
 * Контраст проверяет `src/__tests__/designTokens.test.ts`: текстовые ступени
 * (500 и темнее) держат ≥ 4.5:1 и на белой поверхности, и на самом светлом
 * месте фона (#e6efff).
 */

/**
 * Нейтральная «чернильная» шкала — синевато-серая, как весь макет.
 * 900 — основной текст (#12234a), 500 — вторичный (#55607a), 300 — контур
 * поля (#c9d6ef), 200 — линия-разделитель (#e1e9f7).
 */
const ink = {
  50: '#f5f8fe',
  100: '#edf1f9',
  200: '#e1e9f7',
  300: '#c9d6ef',
  400: '#7c87a0',
  500: '#55607a',
  600: '#465169',
  700: '#364160',
  800: '#23305a',
  900: '#12234a',
  950: '#0b1733',
}

/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        /*
         * Действие. Градиент главной кнопки в макете — #3b7bff → #1f55e0; его
         * светлый край с белым текстом даёт 3.8:1, поэтому начало градиента
         * кнопки взято темнее (`action-from` #2d67f5, 4.8:1), а сплошная заливка
         * действия — 600.
         */
        primary: {
          50: '#eef3ff',
          100: '#dde8ff',
          200: '#c2d5ff',
          300: '#93b4ff',
          400: '#5b93ff',
          500: '#2d67f5',
          600: '#1f55e0',
          700: '#1a46bd',
          800: '#1a3a80',
          900: '#142d66',
          950: '#0e1f47',
        },
        /* Акцент — солнечно-жёлтый #ffc933 (300). Текст на нём только тёмный. */
        gold: {
          50: '#fff8e1',
          100: '#ffefbf',
          200: '#ffe08a',
          300: '#ffc933',
          400: '#ffab00',
          500: '#dc8f00',
          600: '#b06f00',
          700: '#8a5500',
          800: '#6b4200',
          900: '#553400',
        },
        graphite: ink,
        gray: ink,
        slate: ink,
        /* Фон и меню — концы градиентов из макета. */
        canvas: {
          top: '#e6efff',
          mid: '#fdf6e6',
          bottom: '#eaf2ff',
        },
        menu: {
          top: '#1a3a80',
          bottom: '#0e1f47',
          text: '#dfe7fb',
          muted: '#b9c6e6',
        },
        action: {
          from: '#2d67f5',
          to: '#1f55e0',
        },
        /*
         * Состояния проверки (VerdictMark). Цвет несёт смысл, но не один: у
         * каждого состояния своя форма значка, в печати остаётся форма.
         * DEFAULT — значок, `tint` — заливка ячейки/плашки, `ink` — текст на
         * заливке (≥ 4.5:1). Значения — oklch макета, переведённые в hex.
         */
        verdict: {
          ok: { DEFAULT: '#33854a', tint: '#e4f8e7', ink: '#206b38' },
          bad: { DEFAULT: '#b54b43', tint: '#ffe6e2', ink: '#932b27' },
          part: { DEFAULT: '#a97416', tint: '#fdecd1', ink: '#6c4302' },
          unk: { DEFAULT: '#4075aa', tint: '#e8f3ff', ink: '#2c6194' },
          none: { DEFAULT: '#8a857c', tint: '#f1efea', ink: '#5f5a52' },
        },
      },
      fontFamily: {
        sans: ['Manrope', 'system-ui', '-apple-system', 'Segoe UI', 'Roboto', 'sans-serif'],
      },
      borderRadius: {
        card: '20px',
      },
      boxShadow: {
        card: '0 10px 30px rgba(31, 85, 224, 0.08)',
        action: '0 6px 16px rgba(31, 85, 224, 0.28)',
      },
    },
  },
  plugins: [],
}
